# Template for required env vars. Copy to .env.local and fill in real values.
# .env.example IS committed (no secrets); .env.local is NOT.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Invite code required to register (server-only — do NOT prefix with NEXT_PUBLIC_)
INVITE_CODE=
