Explain the code I just wrote or the file given in $ARGUMENTS in plain language.

Focus on:
- What this file/component does and why it exists
- Any React or Next.js concepts used (with a one-sentence explanation of each)
- How it connects to other parts of the app
- What I would need to change if a requirement changed

Assume the reader knows Angular but is learning React. Draw comparisons where helpful.

Arguments: $ARGUMENTS
